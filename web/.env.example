# Copia este archivo como .env y rellena con tus valores reales.
# NUNCA subas el .env real al repo.

PORT=3000

# JWT — genera uno largo y aleatorio (mínimo 32 caracteres)
# Comando para generarlo: openssl rand -base64 48
JWT_SECRET=cambia-esto-en-produccion-por-algo-largo-y-aleatorio

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASS=tu-password
DB_NAME=ecosort